{
  "name": "loofio",
  "private": true,
  "packageManager": "npm@11.11.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm --workspace @loofio/web run dev",
    "build:web": "npm --workspace @loofio/web run build",
    "lint:web": "npm --workspace @loofio/web run lint"
  }
}
